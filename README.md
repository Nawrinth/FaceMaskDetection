# FaceMaskDetection
Real-time Face Mask Detection using CNN and OpenCV. Detects faces via webcam, classifies as “MASK” or “NO MASK,” and displays results with color-coded boxes. Includes dataset preprocessing, model training, and inference scripts for easy use and extension.
